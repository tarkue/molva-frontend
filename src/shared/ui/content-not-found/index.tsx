export const ContentNotFound = ({ name }: { name: string }) => (
  <div className="h-14 w-full">
    <p className="w-full text-center text-subhead text-gray">
      {name} не найдены
    </p>
  </div>
);
